# FluidAudio is pinned in Package.resolved and updated deliberately through
# Xcode/SPM review. This file automates GitHub Actions pins only.

version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
